# BLSVCS

Platform-independent static website for `www.blsvcs.lv`.

## Deployment

The repository is ready for static hosting. For Cloudflare Pages, use:

- Build command: none
- Build output directory: `/`

The custom domain is `www.blsvcs.lv`.
